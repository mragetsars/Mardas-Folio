{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "desktop-default",
  "description": "Capabilities required by the local Mardas Folio desktop window.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "window-state:default"
  ]
}
