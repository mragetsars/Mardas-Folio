import test from"node:test";import assert from"node:assert/strict";import{PRESETS,mergePresetOptions,presetById}from"../frontend/js/core/presets.mjs";test("presets expose stable user choices",()=>{assert.deepEqual(Object.keys(PRESETS),["general","academic","technical","minimal"]);assert.equal(PRESETS.academic.options.quality_profile,"strict-publication")});test("fallback and override are safe",()=>{assert.equal(presetById("x").id,"general");const options=mergePresetOptions("academic",{toc:false,page_size:"Letter"});assert.equal(options.quality_profile,"strict-publication");assert.equal(options.toc,false);assert.equal(options.page_size,"Letter")});
